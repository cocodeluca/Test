import assert from 'node:assert/strict'; import test from 'node:test'; import { readFileSync } from 'node:fs';
test('Expenses is a first-class workspace route using existing navigation conventions',()=>{const sidebar=readFileSync('src/platforms/web/components/Sidebar.tsx','utf8');const renderer=readFileSync('src/platforms/web/components/AppPageRenderer.tsx','utf8');const workspace=readFileSync('src/common/utils/workspace.ts','utf8');assert.match(sidebar,/expenses:.*nav\.expenses/);assert.match(renderer,/case 'expenses'/);assert.match(renderer,/<ExpensesPage/);assert.match(workspace,/expenses: 'Expenses'/);});
test('property details render compact expense history without replacing rent history',()=>{const card=readFileSync('src/platforms/web/components/PropertyCardExpanded.tsx','utf8');assert.match(card,/<RentHistory/);assert.match(card,/<ExpenseHistory/);});
